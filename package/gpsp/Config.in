config BR2_PACKAGE_GPSP
        bool "gpsp"
	depends on BR2_INSTALL_LIBSTDCPP
        help
          ARM-optimized gameboy emulator by notaz

comment "Gpsp needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
