config BR2_PACKAGE_POCKETSNES
        bool "pocketsnes"
	depends on BR2_INSTALL_LIBSTDCPP
        help
          ARM-optimized SNES emulator

comment "Pocketsnes needs a toolchain w/ C++"
	depends on !BR2_INSTALL_LIBSTDCPP
